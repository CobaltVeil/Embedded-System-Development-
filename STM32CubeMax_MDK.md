### 1 在STM32CubeMax配置EXTI外部中断项目中，出现设置抢占优先级，造成反向中断嵌套调用造成程序卡死的问题。
系统默认生成System Tick Timer的抢占优先级为15，EXTI 0 Line一般由用户自行设置，在外部中断回调函数中如果写：
void HAL_GPIO_EXTI_Callback(uint16_t GPIO_Pin)
{
    if(GPIO_Pin == USER_KEY_Pin)
    {
        HAL_Delay(10);
        HAL_GPIO_TogglePin(GREEN_LED_GPIO_Port, GREEN_LED_Pin);
    }
}
HAL库延时函数HAL_Delay使用的是系统滴答定时器作为时间基准，而系统滴答定时器同为中断，在外部中断中触发系统滴答定时器中断会涉及中断优先级的问题，
结论：尽量不要在外部中断回调函数中调用低优先级中断函数，在外部中断回调函数中尽量少的去执行功能，可以通过设标志位再在主循环中执行的方式来处理。

### 2 在STM32CubeMax配置定时器项目中，如何根据定时时间计算预分频寄存器、自动重装载寄存器的值。
#### 2.1 关于定时器所在总线频率与预分频寄存器、自动重装载寄存器、预计定时时间几者之间的关系为 ：
    T = (PSC+1)(ARR+1)/APBX ，上述公式意味着根据定时器所在APB总线频率进行规划，考虑到ARR(自动重装载寄存器)的最大计数取决于所用定时器的位数(目前使用平台有16位和32位)，2的16次方为65536，即ARR选值不能超过(65536-1)。以此为前提APBX/(PSC+1)得到的是定时器计数频率，代表多长时间记一次数(APBX为84MHZ时，(PSC+1)为84时,为1000000HZ，代表1us计数一次；(PSC+1)为84000时,为1000HZ代表1ms计数一次，)。根据需要定时的时间来参考(ARR+1)的值，如想要定时500ms时((PSC+1)为8400，APBX为84MHZ)，得到(ARR+1)的值为5000，从而实现500ms定时。

#### 2.2 这里我提出一个问题，定时时间一定，APB总线频率一定的情况下，预分频寄存器和自动重装载寄存器各自的值越大或者越小有什么效果？
PSC（预分频器）和 ARR（自动重装载值）的分配，本质上是在时间精度和系统资源之间做权衡，简单来说是这样的：
PSC越小，ARR越大：定时精度越高，但需要频繁处理中断或占用更大的计数器范围。
PSC越大，ARR越小：定时精度越低，但更节省CPU资源（中断次数少）或适应小容量定时器。
##### 补充，PSC与ARR值关于中断占用的误解
    在以上PSC与ARR权衡中，关于定时精度对应中断频繁程度的情况，定时精度高意味着计数频率高，定时精度低意味着计数频率低没有问题，而是否频繁占用处理中断取决于是什么类型的中断。溢出型中断因为定时的值是一定的，只在定时到达时溢出产生中断，所以中断次数固定不受定时精度影响。非溢出功能（比较/捕获/PWM）的触发频率与计数频率正相关，定时精度高时，每个比较点都需要处理一次中断，CPU比较忙；而定时精度低时，比较点分的比较开，可选比较点少，更节省CPU资源。

#### 2.3 补充除了基本定时器功能外的通用定时器功能
相比基本定时器来说，从单个通道变成了四个通道，其每个通道均可以实现 **① 输入捕获、② 输出比较和 ③ PWM波生成三种功能 ④ 定时器同步** 即一个定时器可以同时做多件事。
①
②
③ 分析定时器的PWM功能，首先要了解什么是PWM(Pulse Width Modulation脉冲宽度调制)，简单来说就是，通过快速切换数字信号的开关状态，并控制“开”和“关”的时间比例，来得到一个等效的模拟效果。
描述PWM主要有三个关键参数：**极性(可以理解为配置参数)、占空比、频率**；
两种模式：PWM1、PWM2，以向上计数为例，PWL1模式，当CNT < CCR时，输出有效电平；当CNT >= CCR时，输出无效电平。PWM2模式，当CNT < CCR时，输出无效电平；当CNT >= CCR时，输出有效电平。
极性：决定“**有效电平**”到底是什么。(极性高（High）：有效电平就是高电平（如3.3V），无效电平就是低电平（0V）。极性低（Low）：有效电平就是低电平（0V），无效电平就是高电平（3.3V）。)
##### 模式（PWM1 PWM2） + 极性（High Low） = PWM波形
占空比：一个周期内，有效电平占整个周期时间的百分比。
频率：指PWM信号一秒内完成的周期数，单位是Hz（赫兹）。核心影响：频率决定了控制的响应速度和平滑度。频率越高，输出的波形越平滑，但开关损耗也相应增加。软件PWM中：频率越高，意味着CPU需要更频繁地进入中断来模拟波形，因此CPU负载会显著增加。硬件PWM中：波形由硬件自动产生，频率变化不会带来额外的中断开销，对CPU负载没有影响。
##### 硬件PWM与软件PWM有什么区别？通过STM32CubeMax配置定时器设置PWM输出属于软件PWM还是硬件PWM？
**直接回答**：通过STM32CubeMax配置定时器设置的PWM输出，属于**硬件PWM**。
判断标准很简单：**看波形持续产生时，是谁在干活？**
##### 1. 硬件PWM（当前的使用方式）
- CPU的角色：只负责**前期配置**（设置PSC、ARR、CCR）和**启动命令**（HAL_TIM_PWM_Start()）
- 硬件的作用：启动后CPU彻底撒手，**定时器硬件独立完成**：
  - 计数器自动运行 - 电平自动翻转 - 占空比精确维持
- 结果：即使CPU去睡觉，PWM波形依然稳定输出
##### 2. 软件PWM
- CPU的角色：**全程亲力亲为**
- 工作方式：用高频定时器中断，每次中断里CPU亲自去：
  - 翻转GPIO引脚 - 维护计数值 - 控制占空比
- **结果**：CPU被绑死在高频中断里，难以处理其他任务
##### 3. 比喻
- 硬件PWM：像自动钢琴——你按播放键后，钢琴自己弹，你可以去喝茶
- 软件PWM：像手动弹琴——每个音符都要你亲手按，完全不能分心
##### 4.推理与使用
一句话总结：你用STM32定时器做的PWM，是让硬件替你干活；软件PWM，是你自己替硬件干活。
同样，所有可用软硬件实现的功能都遵循同一逻辑：
**硬件控制 = 配置专业外设替你干活，CPU解放；软件控制 = CPU自己充当那个外设**，亲力亲为。前者是“指挥”，后者是“执行”。
使用软件还是硬件，取决于场景，核心是**权衡性能与成本**：
- 硬件方案：需要相应外设存在。优点是精度高、功耗低、CPU零负担；缺点是灵活性受限（引脚/功能固定）
- 软件方案：可快速实现任意引脚。优点是灵活；缺点是精度受限、耗费CPU性能、CPU被完全占用









